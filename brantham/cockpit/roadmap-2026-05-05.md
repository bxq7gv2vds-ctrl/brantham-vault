---
date: 2026-05-05
project: brantham
type: roadmap
tags: [cockpit-web, audit, optimisation, M&A]
status: active
---

# Cockpit Brantham — Roadmap 20 étapes (2026-05-05)

Issue de l'audit transverse 2026-05-05 (4 agents parallèles : coverage marché, workflow process, sourcing acquéreurs, qualité produit). Verdict global : plateforme à ~40% du potentiel, vendable single-tenant à 2k€ setup + 200€/mois ; SaaS scalable = 2-3 mois additionnels.

## Contexte audit

| Axe | Score | Constat |
|---|---|---|
| Coverage marché | 50-60% | 33 AJ scrapés, 8 cassés silencieusement, BODACC absent, cron pas chargé |
| Workflow process | 45% | 6 phases off-platform : extension NDA, LdM, teaser, NDA repreneur, mails, data room |
| Sourcing acquéreurs | 25% | Pipeline solide bloqué : 0 NAF en DB réelle, PAPPERS_API_KEY absent |
| Qualité produit | 4/10 | Path hardcodé, 0 auth, 0 multi-tenant, 0 tests, RepreneursPanel monolithe 1448 lignes |

## Bloc A — Quick wins infra (jour 1, ~2h)

### 1. Activer le cron daily-scrape ✓ FAIT 2026-05-05

`launchctl load ~/Library/LaunchAgents/com.brantham.daily-scrape.plist`. Job tourne à 8h chaque jour, alimente Supabase via `cockpit.import_scan`. Distinct de `com.brantham.scrape-enrich` qui alimente l'ancien dashboard (toutes les 3h).

**Bug fixé** : `daily_scrape.sh:30` passait `import_scan "$SCAN"` (positionnel) alors que la signature attend `--scan "$SCAN"` (flag). Corrigé.

**Résultats live** : actives 148 → **188** (+40 new), last_scrape_at 2026-04-26 → 2026-05-05, new_24h 0 → 40.

**Blocage TCC restant** : launchd ne peut pas exécuter dans `~/Downloads/` (Operation not permitted). Le job échouera à 8h demain. **Solution** : anticiper l'étape 16 et déplacer `~/Downloads/brantham-pipeline/` → `~/brantham-pipeline/` (path hardcodé aussi dans `cockpit-web/app/api/hunters/stream/route.ts:5`).

### 2. Purger les DLDO passés ✓ FAIT 2026-05-05

Migration `007_purge_dldo_expired.sql` : ajoute colonne `auto_archived_at TIMESTAMPTZ` + index partiel.

Script `cockpit/cron_purge_expired.py` : set `user_status='archive'` + `auto_archived_at=NOW()` quand `date_limite::date < CURRENT_DATE - 7j` ET `user_status='nouveau'` (préserve shortlist/contacted/replied/poubelle/déjà-triés). Idempotent. Flag `--dry-run` et `--days N` dispos.

Intégré à `daily_scrape.sh` après l'import. Tournera donc quotidiennement.

**Résultat** : 263 opps archivées en 1 run. Filtre Vue "⌫ archivées" les retrouve toutes (déjà existant côté UI, juste plus peuplé maintenant).

### 3. Ajouter clés API au `.env` pipeline

- `PAPPERS_API_KEY` (100 tokens/mois gratuit, MCP installé)
- `HUNTER_IO_KEY` (25 emails/mois free)
- Débloque enrichissement contact Soren

### 4. Fix hydration mismatch `Cockpit.tsx:58`

`new Date().toLocaleString()` rendu SSR vs CSR diffère d'1s → React refait tout le tree. Fix : `useState("")` + hydrate via `useEffect`. ~5 lignes.

## Bloc B — Data quality (semaine 1, ~2j)

### 5. Câbler `enrich_v2.py` post-scrape

Hook fin de `daily_scrape.sh` : SIREN lookup Pappers → NAF + CA + effectif + dirigeant. Cible NAF coverage 0% → 80%. Outil : Pappers MCP ou REST.

### 6. Scraper BODACC API officielle

Source légale data.gouv `bodacc-rcs-a` (RJ/LJ exhaustif). Estimation +30-40% coverage. Outil : `requests` + JSON public.

### 7. Réparer 8 scrapers cassés

BVP (nouvelle URL), Adjust, KSG, SAJ, AJ Partenaires, Ascagne, Cardon, Maydaymag. Debug existant `scraper_aj.py`.

## Bloc C — Génération documents (semaine 2, ~3j)

### 8. Générateur teaser ★ BLOQUANT MAGIC FORM J-16

Bouton "Générer teaser" dans `OppDrawer` → fusion template `teaser-patterns.md` + champs opp → preview MD → export PDF. Outils : `unified` + `remark` (MD), `puppeteer` ou `react-pdf` (PDF). Templates en DB pour customisation.

### 9. Générateur lettre de mission (LdM)

Bouton "Mandater" dans `DealDrawer` → fusion template `lettre-de-mission.md` + deal.fees + repreneur info → PDF. Table `mandates` (deal_id, type ldm/extension_nda, status, signed_at, pdf_url).

### 10. Générateur courrier extension NDA AJ

Bouton "Demander extension NDA" dans `OppDrawer` → fusion template + opp + AJ contact → PDF + mailto pré-rempli.

## Bloc D — Communication intégrée (semaine 3, ~3j)

### 11. Templates emails centralisés

Table `email_templates` (slug, subject, body MD, vars). Modal "Composer email" RepreneursPanel : template + preview + envoi via Gmail API (au lieu de mailto:). Outil : Gmail MCP déjà installé.

### 12. Tracking ouverture email

Pixel `<img src="/api/track/open?token=...">`. Table `email_events` (email_id, type sent/open/reply, ts). Update auto `repreneurs.replied_at` via Gmail webhook ou polling.

## Bloc E — Robustesse (semaine 4-5, ~5j)

### 13. Découper `RepreneursPanel.tsx` (1448 lignes)

Split en 6 fichiers : `RepreneursPanel`, `RepreneurCard`, `HuntControls`, `ScoreBar`, `EmailComposer`, `helpers/buildMailto.ts`.

### 14. Setup tests

Vitest + React Testing Library + Playwright. Unitaires sur fonctions pures (`parseBreakdown`, `dldoCellClass`, scoring). E2E Playwright sur 3 flows : promote opp→deal, hunt repreneurs, mark contacted.

### 15. Sentry + activity log

Sentry browser + Next server. Table `activity_log` normalisée (replace events) : actor, target_kind, target_id, action, payload, ts. Outil : `@sentry/nextjs`.

## Bloc F — Path déploiement (semaine 6-7, ~5j)

### 16. Sortir pipeline Python du `child_process`

Path `/Users/paul/Downloads/brantham-pipeline` hardcodé `hunters/stream/route.ts:5` = bloquant déploiement. Solution : pipeline en service séparé (FastAPI worker) + Redis BullMQ via Next API routes.

### 17. Variables d'env propres

`PIPELINE_URL`, `PIPELINE_API_KEY` au lieu de path local. `.env.example` complet. Validation via `zod`.

## Bloc G — Multi-tenant SaaS (mois 2-3, ~2-3 sem)

### 18. Auth Clerk

Multi-user (Paul + Soren minimum). Table `users` + `org_members`. Middleware Next.

### 19. Multi-tenancy DB

Colonne `org_id` partout (`opportunities`, `deals`, `repreneurs`, `todos`, `mandates`). RLS Supabase activé. Migration lourde, mode read-only un temps.

### 20. Billing Stripe

Plans (Solo 200€, Team 500€, Enterprise sur devis). Webhooks Stripe → table `subscriptions`. Portail client Stripe.

## Ordre d'exécution suggéré

| Période | Étapes |
|---|---|
| Cette semaine | 1-4 (quick wins) puis 8 (teaser pour Magic Form J-16) |
| Semaine 2 | 5-7 (data quality) |
| Semaine 3 | 9-10 (LdM + extension NDA) |
| Semaine 4 | 11-12 (mails intégrés) |
| Mois 2 | 13-17 (robustesse + déploiement) |
| Mois 3 | 18-20 (SaaS-ready) |

## Related

- [[brantham/_MOC]]
- [[brantham/sessions/2026-05-05-audit-cockpit-roadmap]]
- [[brantham/sessions/2026-04-28-cockpit-minimalisme-outreach]]
- [[brantham/deals/active/magic-form-levallois/_MOC]]
- [[brantham/context/process-end-to-end]]
- [[brantham/context/realite-business]]
- [[brantham/templates/lettre-de-mission]]
- [[brantham/templates/courrier-extension-nda-aj]]
- [[brantham/patterns/teaser-patterns]]
- [[_system/MOC-decisions]]
